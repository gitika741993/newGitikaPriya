pipeline {
    agent any

    stages {
        stage('GitPipeline') {
            steps {
                git credentialsId: 'Git', url: 'https://github.com/gitika741993/newGitikaPriya.git'
            }
        }
    }
}

